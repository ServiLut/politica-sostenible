# Recovery candidate, 21 September 2026

This branch repairs the combined image and migration/runtime contracts. It does
not contain credentials and does not modify any published migration. Production
deployment remains conditional on the reviewed environment, preserved backups,
and explicit approval of the immutable platform administrator ID.

## Evidence from the isolated preserved-data rehearsal

The source was the stopped, fully restored local PostgreSQL copy `pg-restore-07`.
The new copy was verified byte for byte, renamed to
`politica_recovery_20260921`, and run with network `none`, no published ports,
and no background replication/archive/network workers. The application and its
Redis shared this isolated network namespace. All test secrets and the unused
platform administrator UUID were synthetic; none may be promoted to production.

- The first nine successful migration checksums match canonical Git bytes.
- The failed `20260905130000_subscription_plans` attempt has zero applied steps,
  original BOM checksum
  `2014782367f42cb15389ab47b032c8c5d0b22aa6ece8d5eed3f59aa48503a450`, and SQLSTATE
  42601 at the initial BOM. Its two tables and three enum types were absent.
- Prisma 7.9.1 `migrate resolve --rolled-back` succeeded. This preserves the
  failed history row; it does not roll back database DDL.
- All 33 formerly pending migrations applied. Two new transactional migrations
  add the missing schema objects and update the contract marker. No published
  migration was rewritten and no `db push`, reset, drop, or data deletion was used.
- Final Prisma status and schema diff passed, as did strict database identity,
  plan catalogue, constraints, triggers, and function search-path checks.
- All 194 original tables were compared by row count and deterministic hashes of
  every original column before migrations and again after the application ran.
  The only changed original table is `_prisma_migrations` (10 to 45 rows: the
  retained failed attempt plus 44 successful migrations). All other 193 original
  tables preserve every original row value. Original primary-key fingerprints
  also match. New catalogue/metadata rows are four plans and one database identity.
- The stopped source cluster files remain byte-identical to their initial hashes.
- Real API `/health/live` and `/health/ready`, web `/api/health/ready`, `/`, and
  `/iniciar-sesion` return HTTP 200. Worker heartbeat is valid with separated UIDs,
  read-only application filesystem, no-new-privileges, and only SETUID/SETGID.
- 58 focused Node tests and 13 Nest/Jest readiness/heartbeat tests pass.
- A pre-existing `migration-files.test.mjs` atomicity check fails for published
  `20260909320000_transition_handover_reports`, which lacks an explicit transaction.
  It was preserved unchanged and applied successfully in the isolated rehearsal.
  The two recovery migrations are transactional. Do not rewrite deployed history
  to hide this existing test failure.

## Changes

The image now includes both worker entry modules, the compiled worker, and its
UID/GID. Its timestamp-only heartbeat is readable by the supervisor while only
the worker can write it. The migrator and readiness use the same current schema
marker. Known previous markers may be upgraded but cannot pass final readiness.
Plan checks match the already published seed migration and billing service;
the recovery does not change commercial plan data.

The additive schema reconciliation creates the empty `TerritoryLeader` table,
its tenant-scoped foreign keys, missing indexes, and `pg_trgm`; three existing
indexes are renamed to match the Prisma contract. The new marker preserves the
database fingerprint and rebinds trigger functions to the active schema.

## Controlled production sequence

1. Preserve a fresh logical dump, globals/roles, schema-only dump, and a read-only
   inventory of every production business table before migration. Use the exact
   production table list observed by the root recovery task (not a hardcoded
   guessed count). Save original-column and primary-key fingerprints, migration
   history, plan catalogue state, and database identity. Verify dump completeness,
   sizes, checksums, and restore evidence. Keep the existing preserved originals.
2. Freeze this candidate commit. Build with the existing public origins/key and
   the exact repair commit revision. Keep auto-deploy and domain routing disabled
   until the candidate passes readiness. Do not change the default branch.
3. Validate production URL targets without printing their credentials: expected
   database `postgres`, schema `politica-sostenible-crm`, established server;
   DIRECT_URL and DATABASE_URL must refer to the same actual database/schema.
   Verify failed history again: one active failed attempt, exact BOM checksum,
   zero steps, original two tables/three enum types absent, and the first nine
   checksums equal to the frozen source. Stop if any fact differs from rehearsal.
4. Keep the old application from racing with the migration, without restarting
   PostgreSQL or unrelated services. Build the candidate without starting it.
   Using only the reviewed Compose environment, the official resolve command is:

   ```sh
   docker compose -f compose.recovery.yml run --rm --no-deps \
     --workdir /app/apps/api --entrypoint node app \
     node_modules/prisma/build/index.js migrate resolve \
     --rolled-back 20260905130000_subscription_plans
   ```

   This command must run only after steps 1–3 pass. Do not mark the failed attempt
   applied. Do not manually edit Prisma checksums or history.
5. Execute the candidate's guarded migrator as a one-shot before serving traffic:

   ```sh
   docker compose -f compose.recovery.yml run --rm --no-deps \
     --entrypoint node app --input-type=module --eval \
     "import {runSafeMigrations} from './deploy/migrate.mjs'; await runSafeMigrations();"
   ```

   Require exit 0, 44 successful migrations, no active failed attempt, empty
   schema diff, current marker `20260921182000_schema_contract_marker`, and all
   strict checks passing. Preserve the console result and new dump/checksums.
6. Recalculate fingerprints with the **original** column lists so added columns
   do not cause a false mismatch. All original application row values and PKs
   must match; `_prisma_migrations` is the expected exception. If unrelated live
   services wrote to their own tables during the comparison, account for those
   documented legitimate writes rather than claiming exact equality blindly.
   Never automatically restore a whole shared database to address one mismatch.
7. Start private Redis and the candidate app. Verify worker heartbeat, API live
   and ready, proxied web readiness, homepage and `/iniciar-sesion`. Verify Redis
   persistence, expected filesystem/capabilities/UIDs, and actual Storage access
   independently with existing credentials. The local test does not prove remote
   Storage connectivity because external networking was intentionally absent.
8. Only then assign the established domains to the candidate port 3000 through
   Dokploy/Traefik and verify HTTPS from outside. Keep original configuration,
   image references, volumes, and backups for recovery. No destructive rollback
   of the shared PostgreSQL database is authorized by this document.

## Runtime configuration

`compose.recovery.yml` names the exact required variables. Preserve existing
DATABASE_URL, DIRECT_URL, schema, JWT secret, consent salt, Supabase origin,
service key, bucket and public key. The current code has no runtime references
to NEXTAUTH_SECRET/NEXTAUTH_URL; auth goes through the API with JWT_SECRET.

Missing configuration must be provisioned deliberately: a private Redis password
(64 random hexadecimal characters), OFFLINE_SYNC_HMAC_SECRET (at least 32 random
bytes), an explicitly approved immutable SAAS_ADMIN_USER_IDS value, and MFA key
ID plus a 32-byte canonical base64 encryption key. Preserve any existing MFA keys
if later evidence finds them. The preserved application contains one active
tenant ADMIN and no stored MFA secrets; a tenant ADMIN role alone does not prove
authorization for platform administration.

For the established `evaluation` profile with PostgreSQL plaintext, the code
requires ALLOW_INSECURE_DATABASE_CONNECTION=true, DATABASE_SSL=false and
DATABASE_SSL_REJECT_UNAUTHORIZED=false together. This is the existing profile,
not a new recommendation for production security. Internal Redis plaintext is
limited to the private Compose network and explicitly enabled. Expose neither
Redis nor PostgreSQL through this Compose file. No external messages, webhooks,
pilot actions, user creation, or data seed is part of this recovery procedure.
